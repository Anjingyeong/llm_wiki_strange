{
  "name": "smart-safety-llm-wiki",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:index && vite --host 0.0.0.0",
    "generate:index": "node scripts/generate-search-index.mjs",
    "build": "npm run generate:index && tsc -b && vite build",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@vitejs/plugin-react": "4.7.0",
    "typescript": "^5.9.3",
    "vite": "6.3.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.26",
    "@types/react-dom": "^18.3.7"
  }
}
